\section*{Reproducibility}

All metrics map to stored JSON outputs indexed by deterministic run tags. Code, SLURM configs, and evaluation pipelines provided. Tables regenerate from frozen data via \texttt{run\_stat\_tests.py} and \texttt{generate\_tables.py}. Safety probe checkpoints for both architectures released.
